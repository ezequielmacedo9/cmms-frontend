# Vercel environment variables (set in Vercel dashboard → Project Settings → Environment Variables)
# These are injected at build time via scripts/set-env.js

# Google OAuth Client ID (from Google Cloud Console → Credentials)
NG_APP_GOOGLE_CLIENT_ID=719806847764-XXXXXXXXXXXX.apps.googleusercontent.com

# Backend base URL (Render deployment URL)
NG_APP_API_URL=https://cmms-backend-8y7h.onrender.com
